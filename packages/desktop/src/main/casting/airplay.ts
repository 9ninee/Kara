/**
 * On macOS, AirPlay targets appear as regular CoreAudio output devices.
 * The user selects them via the standard system audio device picker shown in
 * the renderer's DevicePicker component — no custom protocol needed here.
 *
 * This file is a placeholder for any future macOS-specific AirPlay utilities
 * (e.g. launching the system AirPlay picker programmatically via NSMenuItem).
 */

export {}
